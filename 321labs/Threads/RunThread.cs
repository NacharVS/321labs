using System;
using System.Collections.Generic;
using System.Text;
using System.Threading;

namespace _321labs.Threads
{
    public class RunThread
    {
            /*
        static void Main()
        {
            Threads threads = new Threads();
            
            Thread firstThread = new Thread(new ThreadStart(threads.Max));
            firstThread.Start();
            Thread secondThread = new Thread(new ThreadStart(threads.Sort));
            secondThread.Start();
            Thread thirdThread = new Thread(new ThreadStart(threads.Sum));
            thirdThread.Start();
        }
            */
    }
}
